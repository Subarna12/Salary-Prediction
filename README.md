Project Overview

This project predicts whether an individual earns more than $50K per year using machine learning models. It demonstrates the full ML workflow: data preprocessing, feature engineering, model training, hyperparameter tuning, evaluation, and prediction. Two models were implemented and compared: Logistic Regression and Random Forest.
